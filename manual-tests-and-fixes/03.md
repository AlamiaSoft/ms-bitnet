**Option A first.** That's exactly what we were looking for.

The MCR image is especially compelling because it is **Microsoft-published, already contains the 2B model, and is the artifact behind the Azure demo**. Don't build anything ourselves yet.

I would tell OpenCode:

> Proceed with **Option A only**.
>
> Replace our locally built BitNet runtime with:
>
> `mcr.microsoft.com/appsvc/docs/sidecars/sample-experiment:bitnet-b1.58-2b-4t-gguf`
>
> Do not build BitNet from source. Do not create a GHCR pipeline yet.
>
> First determine the image's exact entrypoint, exposed ports, command-line arguments, model path, and API behavior. Run the image directly and verify whether it provides the same inference/API/UI experience as the Microsoft demo.
>
> Then create the smallest possible `docker-compose.yml` around that existing image.
>
> Acceptance criteria:
>
> ```text
> docker compose pull
> docker compose up -d
> ```
>
> must require **zero compilation and zero source downloads**.
>
> Verify:
>
> * model loads
> * `/v1/models` works
> * `/v1/chat/completions` works
> * browser UI works if included
> * `hi` produces sane output
> * performance is measured
> * Ollama at `localhost:11434` remains untouched
>
> **Do not modify or rebuild the image.**
>
> If the image's API/entrypoint is unsuitable for our intended inference-provider architecture, stop and report exactly why rather than falling back to a local build.
>
> Only after Option A is proven unsuitable should we consider Option B (GHCR build).

One caveat: **the fact that the image is Microsoft's and powers the demo doesn't automatically mean its interface is the one we want.** We should inspect that first.

If it gives us a clean inference endpoint, we've effectively skipped the entire build problem. That's the outcome we want.
