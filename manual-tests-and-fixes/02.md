Yes. **That's the better approach.** Don't build BitNet yourself unless there is no suitable prebuilt artifact.

The desired hierarchy should be:

1. **Official BitNet prebuilt Docker image** → use it.
2. **Official/reputable prebuilt runtime image + downloadable model** → use it.
3. **GHCR image built by a GitHub Action / Colab once** → only if no usable image exists.
4. **Local source build** → last resort.

And your demo is important because **the online demo already proves Microsoft has a working packaged inference environment**. We should first identify exactly what that demo is running and whether its image/runtime is publicly available.

So I would **stop OpenCode again** and give it this much narrower task:

> **Do not build anything. Research the existing BitNet distribution first.**
>
> Inspect Microsoft's current BitNet GitHub repository, Docker configuration, releases, GitHub Container Registry, Hugging Face resources, and the infrastructure behind their online demo.
>
> Determine:
>
> 1. Is there an **official Microsoft BitNet Docker/OCI image**?
> 2. Is there an official image on **GHCR**?
> 3. Is the online demo running a publicly available image/artifact?
> 4. Can we pull that runtime directly and mount/use `BitNet-b1.58-2B-4T`?
> 5. If no official image exists, identify the **closest prebuilt/reproducible artifact** that can be pulled without local compilation.
> 6. Only if no suitable artifact exists, propose a GitHub Actions → GHCR build pipeline.
>
> **Do not run CMake, clone/build BitNet, download source, or modify the current Docker setup yet.**
>
> The target developer experience is:
>
> ```bash
> docker pull <existing-bitnet-image>
> docker run ...
> ```
>
> or:
>
> ```bash
> docker compose up -d
> ```
>
> with **zero local compilation**.
>
> Report the exact image/artifact and its source before making any implementation changes.

That is the right next move.

**We should consume BitNet as a packaged runtime, not become BitNet's build engineer.**
